import React, {useState} from "react"
import _ from "lodash"
import Edit from "../../components/GroupPermissionDetail/Edit"

const GroupPermissionDetail = (props) => {
 return <Edit {...props} />
}

export default GroupPermissionDetail
