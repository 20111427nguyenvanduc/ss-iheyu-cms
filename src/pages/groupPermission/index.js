import React, {useState} from "react"
import _ from "lodash"
import Manage from "../../components/GroupPermission/Manage"

const GroupPermission = (props) => {
 return <Manage {...props} />
}

export default GroupPermission
