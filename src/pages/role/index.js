import React, {useState} from "react"
import _ from "lodash"
import Manage from "../../components/Role/Manage"

const Permission = (props) => {
 return <Manage {...props} />
}

export default Permission
